import React from 'react';
import Settings from '@/app/components/settings/Settings';

const page = () => {
  return (
    <div>
      <div>
        <Settings />
      </div>
    </div>
  );
};

export default page;
